{% extends 'base.html.twig' %}

{% block body %}
    <ul>
        {% for book in books %}
            <li><a href="{{ path('book_show', {'id': book.id}) }}">{{ book.title }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}